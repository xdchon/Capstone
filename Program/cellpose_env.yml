name: cellpose-local
channels:
  - conda-forge
dependencies:
  # Keep this base environment portable across machines.
  - python=3.10
  - pip
  - numpy>=1.24
  - pyyaml>=6
  - pyzstd>=0.16
  - tifffile>=2024.6
  - tqdm>=4.66
